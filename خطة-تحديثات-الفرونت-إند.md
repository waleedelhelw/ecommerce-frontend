# 🗺️ خطة تحديثات الفرونت إند

**تاريخ:** 18 مايو 2026  
**المصدر:** تقرير الـ Backend — تغيير DTOs + ميزات جديدة

---

## 📦 1. تغيير إجباري — تعديل أسماء الحقول (OrderDto)

### ما الذي تغير؟

| Old | New | ملاحظات |
|-----|-----|---------|
| `userId` | `customerId` | غير مستخدم في الطلبات (موجود في Auth/Seller contexts بس) |
| `userName` | `customerName` | **يجب التعديل** في كل مكان |
| `userEmail` | `customerEmail` | **يجب التعديل** في كل مكان |
| `userPhone` | ❌ مكانوش | `customerPhoneNumber` موجود بالفعل — تمام |

### الملفات المطلوب تعديلها

#### مجموعة 1 — Order customer name/email

| # | الملف | السطور | التغيير |
|---|-------|--------|---------|
| 1 | `src/pages/seller/SellerOrdersPage.jsx` | 141-143 | `order.userName` ← `order.customerName` , `order.userEmail` ← `order.customerEmail` |
| 2 | `src/pages/seller/SellerOrderDetailsPage.jsx` | 939, 944 | `order.userName || order.customerName` ← `order.customerName` , `order.userEmail || order.customerEmail` ← `order.customerEmail` |
| 3 | `src/pages/admin/AdminOrdersPage.jsx` | 50 | `row.customerName || row.userName` ← `row.customerName` |
| 4 | `src/pages/admin/AdminOrderDetailsPage.jsx` | 586, 588-591 | `order.customerName || order.userName` ← `order.customerName` , `order.customerEmail || order.userEmail` ← `order.customerEmail` |
| 5 | `src/components/admin/RecentOrdersTable.jsx` | 40 | `order.customerName || order.userName || ...` ← `order.customerName` |

#### مجموعة 2 — Return customer name/email

| # | الملف | السطور | التغيير |
|---|-------|--------|---------|
| 6 | `src/pages/seller/SellerReturnsPage.jsx` | 162 | `returnRequest.userName` ← `returnRequest.customerName` |
| 7 | `src/pages/seller/SellerReturnDetailsPage.jsx` | 580, 583 | `returnRequest.userName` ← `returnRequest.customerName` , `returnRequest.userEmail` ← `returnRequest.customerEmail` |
| 8 | `src/pages/admin/AdminReturnsPage.jsx` | 100-101 | `row.userName` ← `row.customerName` , `row.userEmail` ← `row.customerEmail` |
| 9 | `src/pages/admin/AdminReturnDetailsPage.jsx` | 578, 581 | `returnRequest.userName` ← `returnRequest.customerName` , `returnRequest.userEmail` ← `returnRequest.customerEmail` |
| 10 | `src/components/return/ReturnCard.jsx` | 80-81, 88 | `returnRequest.userName` ← `returnRequest.customerName` |

#### مجموعة 3 — Logs (غير Order لكن نفس التغيير)

| # | الملف | السطور | التغيير |
|---|-------|--------|---------|
| 11 | `src/pages/admin/AdminLogsPage.jsx` | 57 | `row.userName || row.userEmail` ← `row.customerName || row.customerEmail` |

#### مجموعة 4 — Reviews (غير Order لكن احتياطاً)

| # | الملف | السطور | التغيير |
|---|-------|--------|---------|
| 12 | `src/components/review/ReviewCard.jsx` | 15 | `review.userName` ← `review.customerName` (لو الـ Backend غيرها) |

---

## 🆕 2. ميزة جديدة — إنشاء طلب خارجي (Guest Order) للبائع

### الوصف
شاشة أو Modal للبائع عشان يعمل طلب باسم عميل مش مسجل.

### ⚙️ API Service

**ملف جديد/مُعدّل:** `src/api/seller/sellerOrderService.js`

أضف:
```javascript
export const createGuestOrder = async (data) => {
  const response = await API.post('/seller/orders/create-guest', data);
  return response.data.data;
};
```

### 🧩 المكونات المطلوب إنشاؤها

#### 1. `src/components/seller/CreateGuestOrderModal.jsx` (جديد)

**المحتوى:**
- Modal منبثق (Overlay)
- **حقول الإدخال:**
  - `customerName` (نص — إجباري)
  - `customerPhone` (نص — إجباري)
  - `customerAddress` (نص — اختياري)
- **اختيار المنتجات:**
  - زر "إضافة منتج" يضيف row جديد
  - كل row فيها: `productId` (Select من منتجات البائع) + `quantity` ( number input)
  - إمكانية حذف أي row
- **أزرار:**
  - "إنشاء الطلب" (Submit)
  - "إلغاء" (Close)

**السلوك بعد النجاح:**
- عرض رسالة نجاح (toast)
- عرض `trackingUrl` مع زر "نسخ الرابط"
- زر "إرسال عبر واتساب" (يفتح `wa.me` بالرابط)
- غلق المودال

**API calls المطلوبة:**
- `getMyProducts` لجلب منتجات البائع (للـ Select)
- `createGuestOrder` لإرسال البيانات

#### 2. `src/pages/seller/SellerOrdersPage.jsx` — تعديل

- إضافة زر "إنشاء طلب خارجي" في الهيدر
- عند الضغط، يفتح `CreateGuestOrderModal`
- بعد نجاح الإنشاء، يعمل refresh للقائمة

---

## 🆕 3. ميزة جديدة — صفحة تتبع الطلبات (Tracking Page)

### الوصف
صفحة عامة (بدون Auth) أي حد يفتحها يشوف حالة الطلب.

### ⚙️ API Service

**ملف جديد:** `src/api/trackingService.js`

```javascript
import API from './axiosInstance';

export const getPublicTracking = async (token) => {
  const response = await API.get(`/public/track/${token}`);
  return response.data.data;
};

const trackingService = { getPublicTracking };
export default trackingService;
```

### 🧩 المكونات المطلوب إنشاؤها

#### 1. `src/pages/public/TrackingPage.jsx` (جديد)

**المحتوى:**
- يستقبل `trackingToken` من الـ URL
- يعرض:
  - **حالة الطلب** مع `statusDisplay` (عربي) كـ Badge كبير
  - **اسم العميل** (`customerName`)
  - **المبلغ الإجمالي** (`totalPrice`)
  - **قائمة المنتجات** (اسم + كمية + سعر)
  - **Timeline** (Step-by-step مثل OrderTimeline)
- حالات الـ Loading والـ Error (الطلب مش موجود = 404)
- زر "نسخ الرابط" لمشاركته
- زر "مشاركة واتساب"

#### 2. `src/components/order/TrackingTimeline.jsx` (جديد — اختياري)

نفس فكرة `OrderTimeline` لكن مبسطة للعرض العام.

#### 3. `src/router.jsx` — إضافة المسار

```javascript
import TrackingPage from './pages/public/TrackingPage';

// داخل الـ MainLayout children (مع الصفحات العامة)
{ path: '/track/:trackingToken', element: <TrackingPage /> },
```

⚠️ **مهم:** المسار `/track/:token` لازم يكون **بره** أي Protected/Guest Route عشان يشتغل من غير تسجيل.

### 🎨 حالات الـ Status بالعربي

```javascript
const STATUS_MAP = {
  PendingPayment:    'في انتظار الدفع',
  Processing:        'قيد التجهيز',
  ReadyToShip:       'جاهز للشحن',
  Shipped:           'تم الشحن',
  Delivered:         'تم التوصيل',
  Completed:         'مكتمل',
  Cancelled:         'ملغي',
  DeliveryFailed:    'فشل التوصيل',
  ReturnedToSeller:  'رجع للبائع',
};
```

---

## 🔄 4. ترتيب التطبيق (Priority)

| الأولوية | المهمة | الوقت المتوقع |
|----------|-------|--------------|
| 🔴 P0 | تعديل `userName` → `customerName` في كل الملفات (12 ملف) | 30 دقيقة |
| 🔴 P0 | تعديل `userEmail` → `customerEmail` في كل الملفات (7 ملفات) | 15 دقيقة |
| 🟡 P1 | إضافة `createGuestOrder` API Service | 10 دقائق |
| 🟡 P1 | إنشاء `CreateGuestOrderModal` | 1.5 ساعة |
| 🟡 P1 | تعديل `SellerOrdersPage` لإضافة زر الطلب الخارجي | 20 دقيقة |
| 🟢 P2 | إنشاء `trackingService.js` | 10 دقائق |
| 🟢 P2 | إنشاء `TrackingPage.jsx` | 1.5 ساعة |
| 🟢 P2 | إضافة المسار `/track/:token` في الـ Router | 10 دقائق |
| 🟢 P2 | اختبار السيناريوهات | 30 دقيقة |

---

## 🧪 5. سيناريوهات الاختبار

### Test 1 — تغيير الحقول
1. اعمل طلب عادي كـ Customer مسجل
2. افتح الطلب في SellerOrders → لازم يظهر `customerName` مش `userName`
3. افتح تفاصيل الطلب → لازم يظهر `customerName` و `customerEmail`
4. اعمل Return → لازم يظهر `customerName` في Returns

### Test 2 — إنشاء طلب خارجي (Seller)
1. سجل كـ بائع
2. افتح `/seller/orders`
3. اضغط "إنشاء طلب خارجي"
4. املأ: اسم العميل، تليفون (إجباري)، عنوان (اختياري)
5. اختار منتج + كمية
6. اضغط إنشاء
7. لازم يظهر `trackingUrl` مع زر نسخ
8. الطلب يظهر في قائمة الطلبات

### Test 3 — تتبع الطلب (Tracking)
1. خذ الـ `trackingToken` من الطلب الخارجي
2. افتح `/track/{token}` في Incognito (من غير تسجيل)
3. لازم يظهر: اسم العميل، حالة الطلب، المنتجات، المبلغ
4. Timeline يظهر مراحل الطلب
5. غيّر حالة الطلب من البائع → افتح التتبع تاني → يتحدث

### Test 4 — 404 Tracking
1. افتح `/track/abc123` (token مش موجود)
2. لازم يظهر "الطلب غير موجود"

---

## 📁 6. هيكل الملفات النهائي (المتغير فقط)

```
src/
├── api/
│   ├── seller/
│   │   └── sellerOrderService.js          ← (مُعدّل: أضيف createGuestOrder)
│   └── trackingService.js                 ← (جديد)
│
├── components/
│   ├── order/
│   │   └── TrackingTimeline.jsx           ← (جديد - اختياري)
│   └── seller/
│       └── CreateGuestOrderModal.jsx      ← (جديد)
│
├── pages/
│   ├── public/
│   │   └── TrackingPage.jsx               ← (جديد)
│   ├── seller/
│   │   └── SellerOrdersPage.jsx           ← (مُعدّل)
│   └── ... (باقي الملفات المذكورة أعلاه)   ← (مُعدّلة: تغيير أسماء الحقول)
│
└── router.jsx                             ← (مُعدّل: إضافة /track/:token)
```

---

## ⚠️ 7. ملاحظات مهمة

1. **الـ return/returnRequest** لسه بيستخدم `userName` — أكد مع الـ Backend لو غيّروها لـ `customerName` في ReturnDtos برضه
2. **الـ logs** فيه `userName` و `userEmail` — نفس السؤال للـ Backend
3. **الـ reviews** فيه `review.userName` — لو الـ Backend غيرها في ReviewDto
4. **الـ fallback chains** (`customerName || userName`) اتخلص منها بعد التغيير — خلّيها `customerName` مباشرة. لو الـ API مضمون إنه راجع بالاسم الجديد، مش محتاج fallback
5. **الـ Guest Order Modal** محتاج يجيب منتجات البائع عشان الـ Select — استخدم `getMyProducts` الموجود
6. **Tracking page** محتاجة تعمل fetch للبيانات أول ما تتحمل — استخدم `useEffect` + `useParams`
